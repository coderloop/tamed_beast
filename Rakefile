require 'bundler'
Bundler::GemHelper.install_tasks

#begin
  #require "jeweler"
  #Jeweler::Tasks.new do |gem|
    #gem.name = "tamed_beast"
    #gem.summary = "Bullettin Board for Rails 3"
    #gem.files = Dir["{lib}/**/*", "{app}/**/*", "{config}/**/*"]
    #gem.author = "Coderloop"
    #gem.description = "A very simple forum plugin"
    #gem.homepage = "http://www.coderloop.com/savage_beast/"
    #gem.email = "info@coderloop.com"
    #gem.version = "0.0.1"
  #end
#rescue
  #puts "Jeweler or one of its dependencies is not installed."
#end
